// UI Configuration
// Set to true to use the Scandinavian UI theme, false for the classic UI
export const USE_SCANDINAVIAN_UI = true;

// You can rebuild the project after changing this setting:
// cd web && npm install && npm run build
